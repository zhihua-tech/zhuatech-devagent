# DevAgent 工程架构

版权所有 © 2026 上海如静知华信息科技有限公司。

Vue 研发管理端和工程师响应式工作台调用 Spring Boot REST API；任务、沙箱资源、质量复核和反馈证据由服务层统一管理，MySQL 使用 JPA/Flyway 迁移。

`AgentRuntime` 与代码工具解耦。生产执行器应采用临时工作区、网络隔离、命令白名单、密钥扫描、资源上限和审批式推送；社区演示执行器不会操作外部仓库。
